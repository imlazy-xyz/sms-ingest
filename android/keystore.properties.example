# Copy to android/keystore.properties and fill in with your own release
# keystore. Never commit keystore.properties or the .jks/.keystore file it
# points to — both are gitignored. See android/README.md § Install (sideload).
storeFile=release.jks
storePassword=changeme
keyAlias=sms-ingest-release
keyPassword=changeme
