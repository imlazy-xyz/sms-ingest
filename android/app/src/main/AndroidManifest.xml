<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Requested at runtime by setup/SetupScreen.kt. RECEIVE_SMS backs the
         .sms.SmsReceiver registration below; READ_SMS backs
         .sms.SmsBackfillReader's content://sms/inbox query (Phase 6, see
         projects/sms-ingest/docs/android-implementation-plan.md). Backfill
         runs once via .sync.BackfillWorker, enqueued right after setup
         completes (Phase 7, .setup.SetupViewModel's onProvisioned callback). -->
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Normal permission, granted at install (no runtime consent needed).
         Required by crypto/PublicKeyFetcher.kt's GET /v1/public-key and the
         Phase 5 upload client. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:name=".SmsIngestApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.SmsIngest"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.SmsIngest">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Inbox-only per the locked v1 scope. android:permission restricts who can
             deliver this broadcast to the system telephony stack, not arbitrary apps. -->
        <receiver
            android:name=".sms.SmsReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter>
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
