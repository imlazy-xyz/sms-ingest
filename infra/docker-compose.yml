services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: sms_ingest
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - sms-ingest-db-data:/var/lib/postgresql/data

volumes:
  sms-ingest-db-data:
